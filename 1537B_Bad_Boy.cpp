#include <bits\stdc++.h>
#define ll long long
#define ld long double
using namespace std;
int main() {
	ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int t, n, m, a, b;
	string s[60];
	cin >> t;
	while(t --) {
		cin >> n >> m;
		for(int i = 0; i < n; ++ i) cin >> s[i];
	}
	return 0;
}
