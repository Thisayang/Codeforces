#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int main() {
	ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int l, r;
	cin >> l >> r;
	if (l == r) cout << l << endl;
	else cout << 2 << endl;
	return 0;
}