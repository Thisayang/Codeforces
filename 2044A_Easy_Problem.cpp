#include <bits/stdc++.h>

using namespace std;

int solve(int n) {
    cout << n - 1 << endl;
    return 0;
}

int main(void) {
    int t;
    int n;
    cin >> t;
    while (t--) {
        cin >> n;
        solve(n);
    }
    return 0;
}